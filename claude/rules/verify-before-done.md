Do not call implementation work complete based only on edited files. Run the relevant tests, type checks, lint, build, or runtime check and report what was actually verified. Reproduce behavior from the current worktree, not a stale process or another checkout. For user-visible UI changes, perform a visual check when the environment supports it and iterate on obvious defects before stopping.
